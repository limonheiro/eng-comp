import sys
def tratar():
 for i in len(texto):
     if (texto[i]=='Q'):
         j=i;
         for j in texto[j]=='}':
             estados=texto[j];
 print(estados);

with open('arquivo.txt','r') as file:
    texto = file.read();
print(texto);
tratar();
